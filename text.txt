npx tsx -e "(async () => { const { pool } = await import('./server/db.ts'); const bcrypt = (await import('bcrypt')).default; const e='admin@css.com'; const p='password123'; try { const [r] = await pool.query('SELECT * FROM users WHERE email = ?', [e]); if(r.length===0){ console.log('❌ USER NOT FOUND IN DB'); } else { const v = await bcrypt.compare(p, r[0].password_hash); console.log('✅ USER FOUND'); console.log('🔐 PASSWORD MATCH:', v); } } catch (err) { console.error('❌ DB ERROR:', err.message); } finally { await pool.end(); } })()"