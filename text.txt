npx tsx -e "import { pool } from './server/db.js'; import bcrypt from 'bcrypt'; const e='admin@css.com'; const p='password123'; const [r] = await pool.query('SELECT * FROM users WHERE email = ?', [e]); if(r.length===0){ console.log('❌ USER NOT FOUND IN DB'); } else { const v = await bcrypt.compare(p, r[0].password_hash); console.log('✅ USER FOUND'); console.log('🔐 PASSWORD MATCH:', v); pool.end(); }"